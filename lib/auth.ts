/**
 * Admin credentials are server-only. Use env vars:
 *   ADMIN_USERNAME
 *   ADMIN_PASSWORD
 * (no NEXT_PUBLIC_ prefix – never exposed to the client)
 * Auth is handled via POST /api/auth/sign-in and session cookie.
 */
export {}
